version: '3.9'
name: 'frontend'

services:
  main:
    build:
      context: .docker
      dockerfile: Dockerfile
    command: /bin/bash -c "./.docker/start.sh"
    volumes:
      - .:/app
      # - /app/vendor/
    ports:
      - 8001:8000
    depends_on:
      - main_db

  main_queue:
    build:
      context: .docker
      dockerfile: Dockerfile
    command: /bin/bash -c "./.docker/queue.sh"
    depends_on:
      - main_db

  main_db:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: main
      # MYSQL_USER: root
      MYSQL_PASSWORD: root
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./storage/dbdata:/var/lib/mysql
    ports:
      - 33064:3306